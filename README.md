These are my personal VSCode's settings.

Installation instructions: (powershell)
```powershell
Remove-Item -Recurse "$env:APPDATA\Code\User"
git clone git@github.com:viniciusmelquiades/vscodefiles.git "$env:APPDATA\Code\User"
```